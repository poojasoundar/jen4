name: Hello World on Windows

on: [push]

jobs:
  say-hello:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print Hello World
        run: echo Hello, World!
